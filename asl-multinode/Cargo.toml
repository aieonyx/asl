[package]
name = "asl-multinode"
version = "0.1.0"
edition = "2021"
authors = ["Edison Lepiten <aieonyx.eu@gmail.com>"]
license = "Apache-2.0"
description = "ASL M23: Multi-node boot — sovereign mesh boot coordinator for Phoenix Desktop"

[dependencies]
asl-common = { path = "../asl-common" }

[features]
default = []
std = []

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
