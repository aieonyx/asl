# Copyright (c) 2026 Edison Lepiten / AIEONYX
# SPDX-License-Identifier: Apache-2.0
# PL-70 M25: Phoenix-Desktop Protection Domain
# Wraps aiXos Phoenix desktop render loop as a seL4 PD

[package]
name        = "asl-phoenix-desktop"
version     = "0.1.0"
edition     = "2021"
authors     = ["Edison Lepiten <aieonyx.eu@gmail.com>"]
license     = "Apache-2.0"
description = "Phoenix-Desktop PD — sovereign desktop render loop under seL4"

[dependencies]
asl-common = { path = "../../asl-common" }
asl-haniel = { path = "../../asl-haniel" }

[features]
default = []
kani    = []
std     = []

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
