[package]
name = "asl-trustgraph"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "TrustGraph-Gate PD — capability token graph and grant validation"

[dependencies]
asl-common = { path = "../asl-common" }
